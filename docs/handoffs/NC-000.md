# NC-000 handoff — repository bootstrap

## Metadata

- **Task:** NC-000 — prepare the repository for sequential agent work
- **Status:** `completed_with_validation_blocked`
- **Repository:** `Dziuras98/engine-sim`
- **Branch:** `agent/nc-000-repository-bootstrap`
- **Pull request:** `#1` (draft)
- **Base branch:** `master`
- **Base commit:** `85f7c3b959a908ed5232ede4f1a4ac7eafe6b630`
- **PR head before final status update:** `1931bd2148d52a97f4bee8db444dfa6d940bcfed`
- **Date:** 2026-07-17

## Objective and scope

Prepare the unmodified upstream-style `engine-sim` fork for ordered nEXTcAR integration work. This task is limited to repository organization, planning, ignore rules, baseline inspection, and handoff documentation. It does not implement Unreal Engine integration, change simulation behavior, or modify build targets.

## Baseline repository

### Main branch

The repository default and integration branch is `master`.

### Top-level structure at the base commit

- `.github/workflows/` — Windows CMake GitHub Actions workflow.
- `art/` — Blender source artwork.
- `assets/` — engine scripts, themes, part library, and runtime assets.
- `configuration/` — application configuration.
- `dependencies/` — bundled Discord RPC files and CMake-managed submodules.
- `design/` — design material retained from the upstream project.
- `docs/public/screenshots/` — existing public screenshots.
- `es/` — core `.mr` script modules.
- `include/` — public and application C++ headers.
- `scripting/` — Piranha-based script interpreter sources and headers.
- `src/` — simulation, audio, application, and UI C++ sources.
- `test/` — existing GoogleTest unit tests wired into CMake.
- `CMakeLists.txt`, `.gitmodules`, `.gitignore`, `README.md`, `LICENSE` — root project files.

The new `tests/` directory is reserved for future nEXTcAR integration/system tests and intentionally does not replace or rename the existing `test/` directory.

## Languages, build system, and dependencies

### Languages

- C++17 for the simulator, audio path, application, and script adapter.
- C in a small portion of the repository/dependency surface.
- CMake for build configuration.
- `.mr` domain scripts interpreted through the Piranha-based scripting layer.

### Build system and targets

The root `CMakeLists.txt` requires CMake 3.10 and defines:

- `engine-sim` — static simulation library;
- `engine-sim-script-interpreter` — optional static scripting layer;
- `discord` / imported `discord-rpc` — optional Discord integration;
- `engine-sim-app` — Windows desktop executable;
- `engine-sim-test` — GoogleTest executable discovered by CTest.

Default CMake options are `PIRANHA_ENABLED=ON`, `DISCORD_ENABLED=ON`, and `DTV=OFF`.

### Dependencies

Git submodules:

- `delta-studio`
- `simple-2d-constraint-solver`
- `direct-to-video`
- `csv-io`
- `piranha`

Documented/system dependencies:

- SDL2
- SDL2_image
- Boost
- Flex
- Bison

Configure-time/test dependency:

- GoogleTest archive pinned to commit `609281088cfefc76f9d0ce82e1ff6c30cc3591e5` through CMake `FetchContent`.

Bundled dependency:

- Discord RPC headers, source wrapper, license, and Windows `.lib`.

## Changes made

| Path | Change | Reason |
| --- | --- | --- |
| `docs/PROJECT_PLAN.md` | Added | Defines ordered work packages NC-001 through NC-012 and the completion contract. |
| `docs/handoffs/TEMPLATE.md` | Added | Standardizes evidence, commands, risks, and next-agent instructions. |
| `docs/handoffs/NC-000.md` | Added | Records the repository baseline and bootstrap result. |
| `docs/decisions/.gitkeep` | Added | Reserves the architecture decision directory. |
| `docs/benchmarks/.gitkeep` | Added | Reserves the benchmark procedure/result directory. |
| `tools/.gitkeep` | Added | Reserves project automation tooling. |
| `tests/.gitkeep` | Added | Reserves future integration/system tests without touching legacy `test/`. |
| `.gitignore` | Expanded | Ignores common CMake, Visual Studio, editor, OS, log, and temporary outputs. |

No files under `src/`, `include/`, `scripting/`, `assets/`, `es/`, `dependencies/`, `test/`, or `CMakeLists.txt` were modified.

## Build and test status

### Existing documented Windows build

Run from a Visual Studio Developer PowerShell with CMake and required dependencies available:

```powershell
git clone --recurse-submodules https://github.com/Dziuras98/engine-sim.git
cd engine-sim
git checkout agent/nc-000-repository-bootstrap
git submodule update --init --recursive
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release
ctest --test-dir build -C Release --output-on-failure
```

### Existing CI build

`.github/workflows/cmake.yml` triggers for pull requests targeting `master`, uses `windows-latest`, initializes submodules, installs/caches Boost 1.78.0, configures CMake, and builds the Release configuration. The workflow does not currently execute CTest.

### Validation performed during NC-000

```text
gh --version
exit 127 — GitHub CLI is not installed in the execution environment.

git clone https://github.com/Dziuras98/engine-sim.git /tmp/nextcar-engine-sim
exit 128 — environment could not resolve github.com, so a local checkout could not be created.
```

Repository-side validation:

- The branch was compared against `master`: 8 changed files, limited to documentation, directory placeholders, and `.gitignore`.
- Draft PR #1 was created to trigger the existing `pull_request` workflow.
- GitHub reported the PR as mergeable.
- No workflow run and no commit status check were created for either the PR head or synthetic merge commit. This indicates that Actions are unavailable, disabled, or otherwise not executing for this fork; the connector exposes no workflow-dispatch action for starting the existing workflow manually.

Final validation state:

- **Static configuration inspection:** passed.
- **Behavioral diff inspection:** passed; only documentation/placeholders and `.gitignore` changed.
- **Local Windows build:** not run; no Windows checkout/toolchain was available.
- **Local CTest:** not run for the same reason.
- **GitHub Actions build:** not run; no workflow run was created after PR #1 opened.

This task does not claim an unexecuted build as successful. The build definition and all code inputs remain unchanged, so NC-000 introduces no build-system or runtime behavior delta. A Windows build remains a required pre-merge validation step.

## Architecture decisions

None. NC-000 establishes locations for future decision records but does not choose the adapter, threading, audio transport, or Unreal integration architecture.

## Known limitations and risks

- The current project documentation states that the application build is Windows-only.
- The existing CI workflow builds but does not run `ctest`; NC-001 should decide whether test execution belongs in the repeatable baseline.
- GitHub Actions did not start for PR #1 and may need to be enabled manually in the fork.
- The project depends on recursive Git submodules and configure-time network access for GoogleTest.
- The Discord RPC path imports a bundled Windows library, which may constrain toolchain/architecture choices.
- `test/` and `tests/` now coexist by design; future agents must not silently migrate existing tests.

## Instructions for NC-001

1. Start from the accepted NC-000 branch/merge commit on `master`.
2. Create `agent/nc-001-repeatable-release-build`.
3. Read `docs/PROJECT_PLAN.md`, this handoff, `.github/workflows/cmake.yml`, `README.md`, `CMakeLists.txt`, `.gitmodules`, and `dependencies/submodules/CMakeLists.txt` before editing.
4. Ensure GitHub Actions are enabled for the fork or use a Windows machine with Visual Studio Build Tools.
5. On Windows, clone recursively or run `git submodule update --init --recursive`.
6. Run the documented baseline configure, Release build, and CTest commands before changing build configuration.
7. Make the clean-clone Release build repeatable and document exact compiler, CMake, dependency, and architecture requirements.
8. Do not implement the headless runner, WAV export, adapter, Unreal plugin, or simulation changes in NC-001.
9. Store any architecture/toolchain decision in `docs/decisions/` and finish with `docs/handoffs/NC-001.md`.

## Acceptance checklist

- [x] Main branch identified.
- [x] Repository structure inspected and documented.
- [x] Languages, build system, targets, and dependencies identified.
- [x] `docs/decisions/` created.
- [x] `docs/handoffs/` created.
- [x] `docs/benchmarks/` created.
- [x] `tools/` created.
- [x] `tests/` created without disturbing legacy `test/`.
- [x] `docs/PROJECT_PLAN.md` contains NC-001 through NC-012.
- [x] Handoff template added.
- [x] `.gitignore` expanded conservatively.
- [x] No engine behavior or build target changed.
- [ ] Windows Release build result recorded before merge.
- [ ] CTest result recorded by a Windows agent; existing CI does not run tests.
